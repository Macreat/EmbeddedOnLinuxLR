# Build DIR

Stores compiled artifacts (object files and binaries).

## Usage

All `.o` and final binaries (`ctl`, etc.) are placed here during build:

```bash
gcc -Wall -Wextra -std=c11 -c sensor/sensor.c -o build/sensor.o


```

## notes

in this folder are archive the manual compiled files used on SIMULATION.
